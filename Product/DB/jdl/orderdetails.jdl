entity OrderDetails{
	@Id IdOrderDetail Long,
    IdOrder Long,
    IdProductDetail Long,
    Quantity Long,
    Price Long,
    CreatedAt ZonedDateTime,
    UpdatedAt ZonedDateTime,
}

dto * with mapstruct
paginate * with pagination
service * with serviceClass
service * with serviceImpl
